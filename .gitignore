# z88dk / appmake build artifacts
*.nex
*.map
*.o
*.seg
*_CODE.bin
*_BANK_*.bin

# ZX 128K (+zx) build artifacts
*.zxo
*.tap
*.sna
/obj-zx128/
/nexthack128

# appmake generated loaders (.bat) — keep our own scripts
*.bat
!build.bat
!run-next.bat
!run-zx128.bat

# raw extensionless binaries produced by zcc
/nexthack

# editor/OS noise
*.swp
.DS_Store
Thumbs.db

# Claude Code: the skills in .claude/skills/ ARE part of the project (they
# encode the banking + emulator-verification procedures); the local settings
# file is per-machine permission grants and is not.
.claude/settings.local.json

# emulator droppings
zesarux_autosave.zsf
zesarux_index_menu.idx
nexthack.sav
nexthack.sav.bak
nexthack.hi

# python bytecode from the asset converters
__pycache__/
